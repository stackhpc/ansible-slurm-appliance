---

openhpc_passwords:
  openhpc_grafana_admin_password: "{{ openhpc_grafana_admin_password | default(lookup('password', '/dev/null')) }}"
  openhpc_elasticsearch_admin_password: "{{ openhpc_elasticsearch_admin_password | default(lookup('password', '/dev/null')) }}"
  openhpc_mysql_root_password: "{{ openhpc_mysql_root_password | default(lookup('password', '/dev/null')) }}"
  openhpc_mysql_slurm_password: "{{ openhpc_mysql_slurm_password | default(lookup('password', '/dev/null')) }}"
