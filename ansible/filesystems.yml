---

- name: Setup NFS
  hosts: nfs
  become: true
  tags:
    - nfs
  tasks:
    - include_role:
        name: stackhpc.nfs

- name: Mount Openstack Manila share
  hosts: os_manila
  tasks:
    - include_role: os_manila_ceph
