---
- hosts: cuda
  become: true
  gather_facts: true
  tags: cuda_bandwidth
  tasks:
    - name: Run CUDA bandwidth tasks
      ansible.builtin.import_role:
        name: cuda
        tasks_from: bandwidth.yml
