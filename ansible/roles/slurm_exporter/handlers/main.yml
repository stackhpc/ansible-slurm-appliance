---
- name: Restart slurm exporter
  become: true
  systemd:
    daemon_reload: true
    name: prometheus-slurm-exporter
    state: "{{ 'restarted' if slurm_exporter_state != 'stopped' }}"
  when:
    - not ansible_check_mode
