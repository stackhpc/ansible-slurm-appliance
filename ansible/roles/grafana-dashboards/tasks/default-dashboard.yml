- name: Configure default dashboard
  become: true
  community.general.ini_file:
    path: /etc/grafana/grafana.ini
    section: dashboards
    option: default_home_dashboard_path
    value: "{{ grafana_dashboard_default }}"
  when: "grafana_dashboard_default != ''"
  notify: restart grafana
