- name: Denylist kernel modules
  hosts: kernel_modules
  tags: kernel_modules
  gather_facts: false
  become: true
  tasks:
    - ansible.builtin.include_role:
        name: kernel_modules
        tasks_from: runtime.yml
