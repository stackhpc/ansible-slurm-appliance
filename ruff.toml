line-length = 120

# Store the cache in a temporary location
cache-dir = "/tmp/.ruff_cache"

# as for azimuth-cloud python repos, e.g. azimuth-caas-operator
# + pylint rules
[lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # flake8-builtins
    "A",
    # Ruff rules
    "RUF",
    # flake8-async
    "ASYNC",
    # pyupgrade
    "UP",
    # tidy imports
    "TID",
    # sorted imports
    "I",
    # check complexity
    "C90",
    # TODO: fix pep8 naming
    # "N",
    # pylint rules
    "PLE",
    "PLW",
]
ignore = [
    "UP017", # remove and fix once CI not running 3.10 tests
]
