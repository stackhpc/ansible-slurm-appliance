# cinder volume on control node, nfs mounted to others (TODO):
appliances_state_dir: /var/lib/state

node_fqdn: "{{ inventory_hostname }}.{{ openhpc_cluster_name }}.{{ cluster_domain_suffix }}"

# path to shared slurm files on control node:
sussex_shared_slurm_path: /exports/slurm
